# @package _global_

defaults:
  - serenfree_stage2_serenlens_books
  - _self_

tags: ["serenlens_books", "serenfree", "stage3", "aig", "full_sid"]

serenfree_eval:
  checkpoint: ${paths.root_dir}/tmp/checkpoints/amazon_books_serenfree_stage2/last.ckpt
  aig: true
  aig_alpha: 0.5
  aig_levels: adaptive_semantic_non_dedup
  aig_gate_min_acceptable_prob: 0.0
  output_json: ${paths.root_dir}/logs/serenfree_stage3_serenlens_books/retrieval_eval_test.json
